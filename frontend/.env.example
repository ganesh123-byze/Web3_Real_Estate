# Local backend (uvicorn on port 8000):
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
# Vercel / production — set your Render (or other) API origin (no trailing slash).
# NEXT_PUBLIC_API_BASE_URL=https://your-backend.onrender.com
# Vercel also accepts BACKEND_URL or API_BASE_URL (mapped at build via next.config.mjs + prebuild).
NEXT_PUBLIC_CHAIN_ID=11155111
NEXT_PUBLIC_EXPLORER_TX_BASE=https://sepolia.etherscan.io/tx/
